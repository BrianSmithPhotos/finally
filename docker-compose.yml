services:
  finally:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env
    environment:
      - DB_PATH=/app/db/finally.db

volumes:
  finally-data:
